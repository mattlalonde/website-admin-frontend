import * as types from './types';

/*

export const createArticleRequest = (ownerUserId: string, title: string): types.ArticleActionTypes => {
    type: types.ARTICLE_CREATE_REQUEST,
    payload: { title }
}

 interface IUpdateArticlePayload {
    articleId: string;
    body: string;
}

export const updateArticle = (articleId: string, body: string): types.ArticleActionTypes => {
    return {
        type: types.ARTICLE_UPDATE,
        payload: { articleId, body }
    }
} */
